version: '3'
services:
  nats:
    image: nats:latest
    command:
      - "-js"
      - "--tlscert"
      - "${TLS_CERT}"
      - "--tlskey"
      - "${TLS_KEY}"
      - "--tlscacert"
      - "${TLS_CA}"
    environment:
      - "STREAM_NAME=${STREAM_NAME}"
    ports:
      - "4222:4222"
